# Leave unset in local dev — Vite proxy forwards /api and /inspection-observations to port 8080.
# Set this on Vercel (or any production host) to your deployed backend URL.
# VITE_API_BASE_URL=https://your-backend.example.com
